# Checkpoint: 2026-05-20 — Phase 1 Stabilization & UI Foundation

## Current State
- **Backend:** Healthy and responsive on port 8000. Full voice pipeline (STT -> LLM -> TTS) is implemented but awaits real API keys for non-mock testing.
- **Frontend:** Next.js dev server running on port 3000. 
- **Critical Fixes:** 
    - `useAmelieWebSocket.ts` refactored to prevent duplicated listeners and fix the "text repetition" bug.
    - Added safety checks for delta token streaming.
- **UI:** Established a design bridge between the "Soul Orb" and ElevenLabs' editorial aesthetic (Waldenburg 300, Inter 400, atmospheric pastel gradients).

## Pending Tasks for Tomorrow
1. **High-Fidelity UI Overhaul:** Implement the `page.tsx` update using the minimal editorial layout (Large Orb, floating glass input, clean typography).
2. **Functional Test:** Trigger a full conversation loop (Mock or Real) to verify the fix for "text duplication" in the actual UI.
3. **Pillar 3 (Memory):** Verify session summary persistence to SQLite on socket disconnect.

## Environment Note
- Backend requires `GROQ_API_KEY` and `SARVAM_API_KEY` in `.env` for real responses.
- `ffmpeg` is confirmed installed and working for audio normalization.
